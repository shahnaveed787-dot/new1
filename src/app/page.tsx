import { redirect } from "next/navigation";

/** Root redirects to the SEO permalink for Easy and Simple Tree Drawing. */
export default function RootPage() {
  redirect("/easy-and-simple-tree-drawing/");
}
